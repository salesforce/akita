import { createQuery } from '@datorama/akita';
import { {{ camelCase name }}Store } from './{{dashCase name}}.store';

export const {{ camelCase name }}Query = createQuery({{ camelCase name }}Store);
